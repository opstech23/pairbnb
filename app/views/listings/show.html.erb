<%= link_to 'Home', root_path %>

<h1>Show Listing</h1>

	<p>
	<%= @listing.id %>
	<%= @listing.title %>
	<%= link_to 'Edit', edit_listing_path(@listing.id) %>
	<%= link_to 'Delete', 
				delete_listing_path(@listing.id),
				method: :delete,
				data: { confirm: 'Are you sure?' } %>
	</p>